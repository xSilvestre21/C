#include<stdio.h>
int main(void) {
	int qtd, lin, col;
	printf("Digite a quantidade: ");
	scanf("%d", &qtd);
	for(lin = 0; lin < qtd; lin++) {
		for(lin = 0; lin < qtd; lin++) {
			printf("A");
		}
		printf("\n");
	}
	return 0;
}
